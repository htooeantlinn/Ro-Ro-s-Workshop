---
sidebar_label: 'Installing arduino-cli'
sidebar_position: 3
---

import { useState, useEffect } from 'react';

export const ImageLoop = ({ images }) => {
  const [index, setIndex] = useState(0);
  const [isZoomed, setIsZoomed] = useState(false);
  
  useEffect(() => {
    // Only set interval if there's more than one image
    if (images.length <= 1) return;

    const timer = setInterval(() => {
      setIndex((prevIndex) => (prevIndex + 1) % images.length);
    }, 1000);
    return () => clearInterval(timer);
  }, [images.length]);

  return (
    <div style={{ marginBottom: '20px' }}>
      <div 
        onClick={() => setIsZoomed(!isZoomed)}
        style={{ 
          cursor: 'zoom-in', 
          position: 'relative',
          borderRadius: '12px',
          overflow: 'hidden',
          border: '2px solid var(--ifm-color-emphasis-300)',
          boxShadow: '0 8px 24px rgba(0,0,0,0.15)'
        }}
      >
        <img src={images[index]} style={{ display: 'block', width: '100%' }} />
        
        {images.length > 1 && (
          <div style={{
            position: 'absolute', bottom: '10px', right: '10px',
            backgroundColor: '#333', color: 'white',
            padding: '2px 12px', borderRadius: '20px', fontSize: '0.75rem', 
            fontWeight: 'bold', opacity: '0.8'
          }}>
            Step {index + 1} of {images.length}
          </div>
        )}
      </div>

      {isZoomed && (
        <div 
          onClick={() => setIsZoomed(false)}
          style={{
            position: 'fixed', top: 0, left: 0, width: '100%', height: '100%',
            backgroundColor: 'rgba(0,0,0,0.9)', zIndex: 9999,
            display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'zoom-out'
          }}
        >
          <img src={images[index]} style={{ maxHeight: '95vh', maxWidth: '95vw', borderRadius: '8px' }} />
        </div>
      )}
    </div>
  );
};

# Installing arduino-cli
```bash
brew install arduino-cli
```

<ImageLoop images={[
  "https://res.cloudinary.com/dilbjcn76/image/upload/v1773410254/htooeantlinn/pack1/Screenshot_2026-03-10_at_10.13.38_AM_uq32fl.png",
  "https://res.cloudinary.com/dilbjcn76/image/upload/v1773410145/htooeantlinn/pack1/image_15_wja8yf.png"
]} />

```bash
brew upgrade arduino-cli
```

<ImageLoop images={[
  "https://res.cloudinary.com/dilbjcn76/image/upload/v1773410259/htooeantlinn/pack1/Screenshot_2026-03-10_at_10.14.05_AM_hm7okb.png",
  "https://res.cloudinary.com/dilbjcn76/image/upload/v1773410145/htooeantlinn/pack1/image_16_qtfvoi.png"
]} />

```bash
arduino-cli --version
```

<ImageLoop images={[
  "https://res.cloudinary.com/dilbjcn76/image/upload/v1773410259/htooeantlinn/pack1/Screenshot_2026-03-10_at_10.09.13_AM_elyood.png",
  "https://res.cloudinary.com/dilbjcn76/image/upload/v1773410149/htooeantlinn/pack1/image_17_zxroyb.png"
]} />

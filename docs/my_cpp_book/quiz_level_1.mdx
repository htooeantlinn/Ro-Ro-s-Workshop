---
sidebar_label: 'Quiz Level 1'
sidebar_position: 2
---

import { useState } from 'react';

export const ToggleAll = () => {
  const openAll = () => {
    document.querySelectorAll('details').forEach((el) => el.open = true);
  };
  return (
    <div className="admonition admonition-tip alert alert--success" style={{ cursor: 'pointer' }} onClick={openAll}>
      <div className="admonition-heading">
        <h5><span className="admonition-icon">💡</span> Pro Tip: Click here to reveal all answers at once!</h5>
      </div>
    </div>
  );
};

# C++ Quiz: Level 1

<ToggleAll />

### Question 1: 
```cpp
#include <iostream>
using namespace std;

int main() {
    double x = 5, y = 2;
    int ans = x % y;
    cout << ans;
    return 0;
}
```
1. `1`
2. `Compilation Error`
3. `0`
4. `0.5`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 2. Compilation Error**
    > The modulo operator `%` only works with integers in C++. You would need `fmod()` from `<cmath>` for doubles.
  </div>
</details>

---

### Question 2: 
```cpp
int main() {
    int a = 12;
    if (n > 10){
        int x = 9;
        cout << x;
    }
    cout << x;
    return 0;
}
```
1. `99`
2. `9`
3. `Error`
4. `12`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 3. Error**
    > Two issues here: `n` is undefined, and `x` is declared inside the `if` block scope, making it invisible to the final `cout`.
  </div>
</details>

---

### Question 3: 
```cpp
if (false)
    cout << "False";
else 
    cout << "True";
```
1. `True`
2. `False`
3. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 1. True**
    > The condition is false, so it executes the else block.
  </div>
</details>

---

### Question 4: 
```cpp
if (true)
    cout << "False";
else 
    cout << "True";
```
1. `True`
2. `False`
3. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 2. False**
    > The condition is true, so it executes the if block.
  </div>
</details>

---

### Question 5: 
```cpp
if (23)
    cout << "False";
else 
    cout << "True";
```
1. `True`
2. `False`
3. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 2. False**
    > In C++, any non-zero value is true. 23 is true, so it prints "False".
  </div>
</details>

---

### Question 6: 
```cpp
double a = 2;
const i = 2;
i = a * i;
cout << i * 2;
```
1. `4`
2. `5`
3. `8`
4. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 4.Compilation Error**
    > You cannot reassign a value to a const variable (i = a * i).
  </div>
</details>

---

### Question 7: 
```cpp
int a = 5;
cout << a;
int b = 7;
a++; b++;
cout << a << b;
```
1. `568`
2. `560`
3. `550`
4. `Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 1. 568**
    > Prints 5, then increments. a becomes 6, b becomes 8. Final output: 568.
  </div>
</details>

---

### Question 8: 
```cpp
int a = 5;
cout << a;
{
    int b = 7;
    a++; b++;
}
cout << a << b;
```
1. `568`
2. `560`
3. `550`
4. `Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 4. Error**
    > `b` is declared inside a nested scope `{ }`, making it inaccessible to the final `cout`.
  </div>
</details>

---

### Question 9: 
```cpp
int a = 5;
char c = a>2 ? (a<3 ? 'a':'b'):(a>4 ? 'x':'y');
cout << c;
```
1. `a`
2. `b`
3. `x`
4. `y`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 2. b**
    > `a>2` is true, so it checks `a<3`. `5<3` is false, resulting in 'b'.
  </div>
</details>

---

### Question 10: 
```cpp
int x = 2;
bool b = x<10 ? false:true;
cout << b;
```
1. `True`
2. `False`
3. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 2. False**
    > `x<10` is true, so the ternary picks `false`. In C++, `cout` prints `false` as `0`.
  </div>
</details>

---

### Question 11: 
```cpp
#define Integer int;
#define age 18

int main(){
    Integer x = 19;
    if(x>=age)
        cout << "You are eligible to drive!";
    else
        cout << "Sorry! You are not old enough!";
}
```
1. `Error`
2. `You are eligible to drive!`
3. `Sorry! You are not old enough!`
4. `"You are eligible to drive!"`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 1. Error**
    > The macro `#define Integer int;` includes a semicolon, creating a syntax error during replacement.
  </div>
</details>

---

### Question 12: 
```cpp
int a = 6;
switch(a){
    case 1 ... 5: cout << "One to Five\n"; break;
    case 6 ... 8: cout << "Six to Eight\n"; break;
    case 9: cout << "Nine\n"; break;
    case 10: cout << "Ten\n"; break;
}
```
1. `One to Five`
2. `Six to Eight`
3. `Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 2. Six to Eight**
    > `a` is 6, which falls within the range of the second case.
  </div>
</details>

---

### Question 13: 
```cpp
float b = 5;
switch(b){
    case 3: cout << "Three!"; break;
    case 5: cout << "Five!";
    case 7: cout << "Seven!";
    default: cout << "Bye Bye!";
}
```
1. `Five!`
2. `Five!Seven!Bye Bye!`
3. `Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 3. Error**
    > C++ `switch` statements only work with integral types (int, char, enum). `float` is not allowed.
  </div>
</details>

---

### Question 14: 
```cpp
#include<iostream>
using namespace std;
int main(){
    int n = 0;
    switch(n){
        default: n++;
        case 1: ++n; break;
    }
    cout << n;
    return 0;
}
```
1. `0`
2. `1`
3. `2`
4. `Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 3. 2**
    > No case matches 0, so it hits `default`. `n` becomes 1, then falls through to `case 1` where `n` becomes 2.
  </div>
</details>

---

### Question 15: 
Choose correct variable names.
1. `int 3a;`
2. `float -age;`
3. `char father-name;`
4. `int book price;`
5. `double d;`
6. `float areaOfCircle.;`
7. `float scanf;`
8. `int a&b;`
9. `int a1234567890;`
10. `int _@a;`
11. `int _333_;`
12. `char int;`
13. `bool _ = true;`
14. `double ___ = 4.5;`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 5, 7, 9, 11, 13, 14**
    > Valid names must start with a letter or underscore and contain no special symbols or spaces.
  </div>
</details>

---

### Question 16: 
Which of the following are valid in C++?
1. `char a = ‘@’;`
2. `char b = ‘\n’;`
3. `char c = ‘6’;`
4. `char d = ‘13’;`
5. `char e = 65;`
6. `char f = “a”;`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 1, 2, 3, 5**
    > 4 is too long for a `char`, and 6 uses double quotes which denotes a `string`.
  </div>
</details>

---

### Question 17: 
```cpp
#include<iostream>
using namespace std;
int main(){
    int a = 5;
    int b = 2;
    cout << a/b;
}
```
1. `2.5`
2. `2.0`
3. `2`
4. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 3. 2**
    > Integer division truncates the decimal part.
  </div>
</details>

---

### Question 18: 
```cpp
#include<iostream>
using namespace std;
int main(){
    float a = 5;
    int b = 2;
    cout << a%b;
}
```
1. `2.5`
2. `2.0`
3. `1.0`
4. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 4. Compilation Error**
    > The modulo operator `%` cannot be used with `float` types.
  </div>
</details>

---

### Question 19: 
```cpp
#include<iostream>
using namespace std;
int main(){
    float a = 2.0;
    int b = 5;
    cout << b%a;
}
```
1. `2.5`
2. `2.0`
3. `1.0`
4. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 4. Compilation Error**
    > Same as above; modulo requires both operands to be integers.
  </div>
</details>

---

### Question 20: 
```cpp
#include<iostream>
using namespace std;
int main(){
    int a = 1;
    int b = ++a;
    cout << a << b;
    a++; ++b;
    cout << a << b;
}
```
1. `1233`
2. `2233`
3. `2223`
4. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 2. 2233**
    > First, `a` and `b` both become 2. Then they are incremented again to become 3.
  </div>
</details>

---

### Question 21: 
```cpp
#include<iostream>
using namespace std;
int main(){
    int a = 5;
    a += ++6;
    cout << a;
}
```
1. `12`
2. `11`
3. `Compilation Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 3. Compilation Error**
    > You cannot use increment operators (`++`) on a literal constant like 6.
  </div>
</details>

---

### Question 22: 
```cpp
int a = 5;
char A = '5';
cout << a == A;
```
1. `True`
2. `False`
3. `true`
4. `false`
5. `Error`
6. `0`
7. `1`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 6. 0**
    > The ASCII value of '5' is 53, so 5 == 53 is false (0). (Note: Parentheses added for precedence).
  </div>
</details>

---

### Question 23: 
```cpp
int a = 5;
char A = '5';
cout << int(A) == a;
```
1. `True`
2. `False`
3. `true`
4. `false`
5. `Error`
6. `0`
7. `1`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 6. 0**
    > `int('5')` is 53. 53 == 5 is false.
  </div>
</details>

---

### Question 24: 
```cpp
int a = 5;
char A = 5;
bool b = A == a;
cout << b;
```
1. `True`
2. `False`
3. `true`
4. `false`
5. `Error`
6. `0`
7. `1`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 7. 1**
    > Here `A` is assigned the numeric value 5, not the character '5'. So 5 == 5 is true.
  </div>
</details>

---

### Question 25: 
```cpp
cout << (2*3-4/2%1);
```
1. `6`
2. `4.0`
3. `6.0`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 1. 6**
    > Following precedence:  4/2 = 2 ; then  2\%1 = 0 ; then  2*3 = 6 ; finally  6-0 = 6 .
  </div>
</details>

---

### Question 26: 
```cpp
int a = 3.3;
cout << a;
```
1. `Yes`
2. `No`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 1. Yes**
    > Legal, but the decimal is truncated; the output will be 3.
  </div>
</details>

---

### Question 27: 
```cpp
int a = 33;
float b = 3.3;
int c = a+b;
cout << c;
```
1. `36`
2. `36.3`
3. `Error`

<details>
  <summary>Click to see Answer</summary>
  <div>
    **Correct Answer: 1. 36**
    > 33 + 3.3 = 36.3 . Storing this in an `int` truncates it to 36.
  </div>
</details>

---